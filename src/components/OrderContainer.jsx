/*import React from 'react';
import {addPostActionCreator} from '../redux/reducer';
import {connect} from 'react-redux';
import Order from './Order';

let mapStateToProps = (state) => {
    return {
        posts: state.profilePage.posts,
        newOrder: state.profilePage.newOrder
    }
}


let mapDispatchToProps = (dispatch) => {
    return {
        addPost: (Imie, Nazwisko) => {/*dodac zlecenie*/
           /* dispatch(addPostActionCreator(Imie, Nazwisko));
        }

    }
}



const OrderContainer = connect (mapStateToProps, mapDispatchToProps) (Order);

export default OrderContainer;*/
